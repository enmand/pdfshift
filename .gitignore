# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.DS_Store

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.out-race

# Build artifacts
build/
bin/

# Some IDEs still don't fully support go mod
.vscode
.idea
vendor/
